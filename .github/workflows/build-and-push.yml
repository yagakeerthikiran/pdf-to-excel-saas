# ⚠️ DEPRECATED - Use build-and-deploy.yml instead
# This workflow is deprecated and will be removed
# The new unified pipeline handles both build and ECS deployment

name: "[DEPRECATED] Build and Push to ECR"

on:
  # Disabled - use build-and-deploy.yml workflow instead
  workflow_dispatch: # Only allow manual runs for emergency

env:
  AWS_REGION: ap-southeast-2
  ECR_REGISTRY: 654499586766.dkr.ecr.ap-southeast-2.amazonaws.com
  ECR_REPOSITORY_FRONTEND: pdf-excel-saas-frontend
  ECR_REPOSITORY_BACKEND: pdf-excel-saas-backend
  IMAGE_TAG: latest

jobs:
  deprecated-warning:
    name: Deprecated Workflow Warning
    runs-on: ubuntu-latest
    steps:
      - name: Warning Message
        run: |
          echo "⚠️  WARNING: This workflow is DEPRECATED"
          echo "🔄 Please use the 'build-and-deploy.yml' workflow instead"
          echo "🚀 The new workflow provides:"
          echo "   • Unified build and deployment"
          echo "   • Automatic ECS service updates"
          echo "   • Health checks and monitoring"
          echo "   • Better error handling"
          echo ""
          echo "💡 This workflow will be removed in future releases"
          exit 1
